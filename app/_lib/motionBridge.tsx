"use client";
import { motion, useScroll, useTransform } from "framer-motion";
const Motion = motion;
const UseScroll = useScroll;
const UseTransform = useTransform;
export { Motion, UseScroll, UseTransform };
